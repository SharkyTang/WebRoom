# 本轮执行工具记录

本目录保存本次实际使用的快照、隔离副本、日志、保护哈希和浏览器套件编排脚本。路径和 run-id 是本轮实值，不是通用自动化。

再次执行前必须选择全新 run-id/输出目录、核对实际工程和已有服务，不能直接覆盖本目录。快照工具使用独立唯一目录、先归档后重新读取和解包验证；Git历史不在工作区归档内。run-check.py 拒绝覆盖已有日志。

浏览器脚本保持原工程输入行为，只通过原生鼠标/触屏/键盘操作；调试接口只读取。浏览器和本机监听曾需要沙箱外执行许可，不用其他工具绕过。截图和录像来自真实页面，不用生成图或临时页面。
