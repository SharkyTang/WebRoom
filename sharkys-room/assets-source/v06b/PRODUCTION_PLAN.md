# v0.6B 开工与制作边界

2026-09-18。沿用旧三件/A 的暂定风格，用户视觉确认待定。完整快照位置及 SHA-256 见 `validation/v06b/snapshot.json`；这不是仅含旧 HEAD 的回退点。

按钢琴 → iPad/Phone → 垃圾桶/开关 → 键鼠/耳机顺序，每组接入网页并局部回归后再进入下一组。所有材质、几何为项目原创的可辨识近似；使用 `Sharkys_Room_Blockout_Pack/room_master_reference.jpeg` 的物件方向和现有 A/v0.5 画面，不复制参考夜景、UI、品牌字标，不声称精确设备型号。

沿用 `ASSET_BUDGETS.md` 的 B 暂定审查线：60,000 三角面、GLB 增量约 1.2 MB、Hero calls 增量优先 ≤25。不是未经实测的保证或额外授权。重复键帽按材质在同一可动根内合批，屏幕/不同 pivot 不合并；先用无贴图标准 PBR，只有可读性确需才加小纹理。每组记录实物统计，最终用同一 Chrome/SwiftShader、DPR 和相机动作比较前后成本。

保护 FINAL、A16、旧三件、原布局/语义/targets、Hero、钢琴0.65m行程/桌下入口、返回和既有状态。新 VIS 根为 identity；必要支撑只补原悬空占位到现有承托面，不移动原锚点。屏幕类型只扩展 iPad/Phone 占位标题，不做真实页面。无 C、音频、独立灯、昼夜/天气、依赖升级或部署。
